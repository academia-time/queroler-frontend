pre-commit:
  commands:
    branch-name:
      run: sh .lefthook/pre-push-branch-name-check.sh
    format:
      run: npx prettier --write {staged_files}
      glob: '*.{js,jsx,mjs,ts,tsx,css,json,md}'
      stage_fixed: true

    lint:
      run: npx eslint --fix {staged_files}
      glob: '*.{js,jsx,mjs,ts,tsx}'
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  commands:
    branch-name:
      run: sh .lefthook/pre-push-branch-name-check.sh
    validate:
      run: npx tsc --noEmit --skipLibCheck
      glob: '*.{ts,tsx}'

    test:
      run: npm run test:coverage
      glob: '*.{ts,tsx}'
